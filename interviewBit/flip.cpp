#include <iostream>
#include <cstdio>
#include <vector>
#include <cstring>

using namespace std;

vector <int> flip(string A){
	int count_zero;
	int left, right;
	for (int i=0; i < A.size(); i++){
		
	}
}

int main(){
	string s;
	cin >> s;
	vector <int> V;
	V = flip(s);
	return 0;
}